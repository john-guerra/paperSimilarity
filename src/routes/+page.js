import { redirect } from "@sveltejs/kit";
import { base } from "$app/paths";

export async function load({ locals }) {
  // console.log("load", base, `${base}/papers`);
  // if (!locals?.user) {

  // redirect(302, `/papers`);
  // }
}
